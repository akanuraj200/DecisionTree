

Compilation:
	Compilation requires c++2011 standard.

	Compile using the following command:
		"g++ -std=c++11 DecisionTree.cpp -o DecisionTree"
	To run the program, use:
		"./DecisionTree < dataset.txt"
		
			
